import { useState, useEffect } from 'react'
import { useQuery } from '@apollo/client'

/**
 *
 * @param target The id of the 'current' solution we care about.
 */
export const useSolutionQuery = (target: string): void => {}
