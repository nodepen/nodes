import React from 'react'

type GripIconProps = {
  mode: 'input' | 'output'
}

/**
 * Returns svg for the standard grip, its 'shadow', and an invisible buffer target area.
 */
export const GripIcon = ({ mode }: GripIconProps): React.ReactElement => {
  return <></>
}
