import { getComputeConfiguration } from './getComputeConfiguration'
import { getSession } from './getSession'
import { getSolutionStatus } from './getSolutionStatus'
import { getUser } from './getUser'

export const Query = {
  getComputeConfiguration,
  getSession,
  getSolutionStatus,
  getUser,
}
